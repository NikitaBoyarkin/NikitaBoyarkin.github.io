# PRD v8 — задачи

> Исполняемая часть `docs/prd-v8.md`. Каждая задача: что делать, как проверить, кто владелец.
> Критерий приёмки — механический (команда или grep), а не «выглядит хорошо».
> Обновлено: 2026-09-22.

**Легенда владельцев.** `agent` — выполняет Claude. `owner` — выполняет Никита.
`PENDING` — ждёт владельца или внешнего события.

---

## Сводка

| Блок | Задач | Гейт | Оценка |
|---|---|---|---|
| P0-Doc | 5 | **до** гейта | вечер |
| P0-Content | 8 | после гейта | ~неделя |
| P1 | 5 | после P0 | ~неделя |
| P2 | 3 | после P1 | ~2 недели |
| P3 | 4 | когда дойдут руки | — |
| Owner | 6 | — | — |

**Гейт аутрича:** 20 отправленных заявок **ИЛИ** 10 разговоров с решающими людьми.
События пишутся в `docs/contact-log.md`. Без этого P0-Content не начинается.

---

## P0-Doc — гигиена репозитория (до гейта)

### D1 — Убрать упоминания 6 удалённых файлов

**Владелец:** agent · **Файлы:** `CLAUDE.md`, `DESIGN.md`

Удалены, но всё ещё описаны как существующие:

| Файл | Где упомянут | Кем удалён |
|---|---|---|
| `ProjectSpotlight.astro` | `CLAUDE.md:85,86` | редизайн главной (v6) |
| `HomeBoard.astro` | `CLAUDE.md:86,304`, `DESIGN.md:92` | PRD v6 D12 |
| `CaseStudy.astro` | `CLAUDE.md:88,148`, `DESIGN.md` | редизайн кейсов |
| `IntroShader.astro` | `DESIGN.md:77` | `prd-hero-banner-brand-polish.md` |
| `AudienceBar.astro` | `DESIGN.md:92,96,154` | PRD v6 D12 |
| `cv.astro` | `CLAUDE.md:97`, `DESIGN.md:153` | PRD v6 |

**Приёмка:**
```bash
grep -nE "ProjectSpotlight|HomeBoard|CaseStudy\.astro|IntroShader|AudienceBar|cv\.astro" CLAUDE.md DESIGN.md
# ожидается: пусто
```

---

### D2 — Убрать описание несуществующего поля `caseStudy`

**Владелец:** agent · **Файл:** `CLAUDE.md:148`

`CLAUDE.md` документирует поле frontmatter `caseStudy`, которое рендерил `CaseStudy.astro`.
Поля нет в `src/content.config.ts`, компонента нет.

**Приёмка:**
```bash
grep -n "caseStudy" src/content.config.ts   # пусто
grep -n "caseStudy" CLAUDE.md               # пусто
```

---

### D3 — Исправить ложное утверждение в `DESIGN.md:96`

**Владелец:** agent

Строка утверждает, что удалённые с главной компоненты «remain reachable via direct URLs».
Для удалённых файлов это неверно — страниц нет.

**Приёмка:** формулировка не утверждает доступность несуществующих страниц; для
существующих неиспользуемых (D4) — утверждает корректно.

---

### D4 — Зафиксировать 4 неиспользуемых компонента

**Владелец:** agent · **Файл:** `DESIGN.md`

Компоненты существуют, импортов нет:

| Компонент | Импортируется кем |
|---|---|
| `Manifesto.astro` | никем |
| `TopicMap.astro` | никем |
| `ProjectTimeline.astro` | никем |
| `CollaborationFormats.astro` | никем |

**Приёмка:** строка в `DESIGN.md` со списком и пометкой «известный долг, удаление — P3-2».

---

### D5 — Зафиксировать `@paper-design/shaders` как известного сироту

**Владелец:** agent · **Файл:** `DESIGN.md`

Зависимость осталась после удаления `IntroShader.astro`; уже задокументирована в
`docs/prd-hero-banner-brand-polish.md:467`.

**Приёмка:** ссылка из `DESIGN.md` на этот раздел; удаление — P3-3.

---

## P0-Content — доказательства (после гейта)

### C1 — Сверка 30 расхождений с владельцем

**Владелец:** agent + **owner** (подтверждение)

> **История правки (2026-09-22).** Задача называлась
> «Аудит дрейфа по 11 EN-зеркалам» и опиралась на неверный диагноз
> (`docs/prd-v8.md` §2.1). Аудит не сравнивает языки — он диффит против снапшота.

`bun run audit:content` → exit 1. Потери зафиксированы скриптом. По каждому файлу
решение: **восстановить** (цифра-доказательство) или **признать намеренной**
(EN-версия короче сознательно).

Фактически расхождений **30**, по 15 в каждом языке, симметрично. Ниже — EN-половина;
та же таблица справедлива для `projects/*` с теми же числами.

| Файл | Потеряно значений | Приоритет |
|---|---|---|
| `projects-en/volta.md` | 38 (`5.72`×3, `656`×2, `9.2`×2, `9.09`, `227`×2, `44`×2, `0.0001`, …) | 1 |
| `projects-en/sql.md` | 20 | 2 |
| `projects-en/streamlit.md` | 9 (`42`, `5`, `8000`) | 3 |
| `projects-en/games.md` | 7 | 4 |
| `projects-en/posthog.md` | 7 | 4 |
| `projects-en/supabase.md` | 6 (`0.0034`×2, `256`, `32.1`, `37.2`, `5.1`) | 2 |
| `projects-en/ab.md` | 4 | 3 |
| `projects-en/scrolly.md` | 3 | 5 |
| `projects-en/cohort.md` | 1 (`42`) | 5 |
| `projects-en/garden.md` | 2 | 5 |
| `projects-en/sales-calls.md` | 3 | 5 |

**Приёмка:** владелец подтвердил, что удаление метрик из кейсов было намеренным.
Если метрики нужны — возвращать их в **оба** языка, и приоритет задачи восстанавливается.

---

### C2 — Перезаморозка baseline

**Владелец:** agent · **Зависит от:** C1

Рефакторинг `9f7eaa2` был намеренным — значит отстал baseline, а не сломался контент.

**Приёмка:**
```bash
bun run audit:content:snapshot   # baseline → текущее состояние
bun run audit:content            # exit 0
```
Гард продолжает работать: любая **следующая** правка цифр снова даст exit 1.
Либо каждое оставшееся расхождение перечислено как намеренное (см. сообщение скрипта:
«If a change is intentional, re-run `bun run audit:content:snapshot` and say so in the PR»).

---

### C3 — Убрать дословный дубль абзаца

**Владелец:** agent

Один и тот же абзац на двух видимых страницах:

> «Портфолио воспроизводимых аналитических проектов — эксперименты, retention,
> сегментация — с полной методологией и кодом. Создано из проектной работы, а не из найма.»

`src/pages/value.astro:36` и `src/pages/about.astro:57`.

Не трогаем: `src/lib/qa-corpus.ts` (36, 68) — это корпус голосовых ответов, там
дословность уместна, и `CareerSnapshot.astro:27` (fallback-строка).

**Приёмка:**
```bash
grep -rn "Создано из проектной работы, а не из найма" src/pages/
# ожидается: ровно 1 совпадение
```

---

### C4 — Блок «подойдёт, если…» на `/value/`

**Владелец:** agent (черновик) → **owner** (утверждение) · **Зависит от:** —

Формат AJTBD: 3–5 пунктов, каждый — проверяемый признак, не прилагательное.

Плохо: «подойдёт, если нужен ответственный аналитик».
Хорошо: «подойдёт, если в вакансии A/B-тесты и вы ждёте от аналитика дизайна
эксперимента, а не только дашборда».

**Приёмка:** каждый пункт содержит стек, формат или тип задач; ни одного оценочного
прилагательного без критерия. `PENDING: утверждение владельца.`

---

### C5 — Диссертация в основной текст

**Владелец:** agent

Сейчас: `src/pages/index.astro:88-90` — мелкий абзац ниже hero.
PhD по психологии труда — единственный дифференциатор, которого нет у других
кандидатов middle+.

**Приёмка:** строка о PhD читается без скролла на 1440×900.

---

### C6 — Бейдж «ищу роль»

**Владелец:** agent

Явный бейдж: «ищу роль BI / продуктовый аналитик, middle+».

**Приёмка:** виден в первом экране на 375×667 и 1440×900; не ломает контраст-гейт
(`.claude/hooks/contrast-gate.js`).

---

### C7 — Кейс «что не сработало в A/B»

**Владелец:** agent · **Зависит от:** C1 (структура EN-кейсов)

Хотя бы один кейс содержит отменённую гипотезу: что проверяли, что получилось,
почему отменили, какую цифру это стоило.

**Приёмка:** в кейсе есть секция с отменённой гипотезой и числом; формулировка
не превращает провал в победу.

---

### C8 — `/resume` — страница PDF-резюме

**Владелец:** agent (страница) + **owner** (файл PDF) · **Зависит от:** C2

**Приёмка:**
- страница `/resume` существует, отдаёт PDF;
- чек-лист совпадения цифр сайт ↔ резюме пройден:

| Цифра | Сайт | Резюме |
|---|---|---|
| KYC-конверсия | +5.72 п.п. | ☐ |
| Годовой эффект KYC | €656K | ☐ |
| Retention M3 | +9.2 п.п. | ☐ |
| LTV-эффект | €227K | ☐ |
| Концентрация выручки | 12% → 41% | ☐ |
| Отчётность | 2ч → 5 мин | ☐ |

`PENDING: файл PDF от владельца.`

---

## P1 — дистрибуция

| ID | Задача | Владелец | Приёмка |
|---|---|---|---|
| P1-1 | Домен `nikitaboyarkin.com` + 301 со старого | **owner** (покупка, DNS) + agent (конфиг) | 301 с `github.io`; canonical на новом домене |
| P1-2 | Vercel: превью + прод | **owner** (доступ) + agent | превью-URL на PR; прод из `main` |
| P1-3 | Поисковый индекс: EN-посты и части Volta | agent | поиск по заголовку EN-поста находит его; счётчик записей индекса вырос на ~49 |
| P1-4 | Локаль как фильтр поиска | agent | выдача фильтруется переключателем, а не разбита на секции |
| P1-5 | LinkedIn headline + about | **owner** | тексты совпадают по смыслу; ссылка на новый домен |

`PENDING: P1-1 и P1-2 требуют покупки домена и доступа к Vercel.`
`PENDING: P1-5 — правки в LinkedIn делаются владельцем вручную.`

---

## P2 — метрики и персонализация

| ID | Задача | Приёмка |
|---|---|---|
| P2-1 | Переключатель `audience` (рекрутер / коллега-аналитик) | выбор в `localStorage.audience`; analytics уже шлёт super-property (`src/lib/analytics.ts:212-229`) |
| P2-2 | Дашборд воронки в PostHog | визит → кейс → резюме → контакт на 13 существующих событиях |
| P2-3 | Пороги из PRD §7 зафиксированы в дашборде | CTR резюме 5–10%, bounce < 60% |

**Условие отмены P2-1.** Если трафика недостаточно, чтобы оправдать ветвление
контента, задача отменяется, а мёртвые селекторы `.audience-bar` удаляются из
`src/styles/global.css` (строки 276, 294–296, 309, 318, 333, 338).
Компонент `AudienceBar.astro` был удалён в PRD v6 (D12) намеренно — это возврат,
а не восстановление.

---

## P3 — долг

| ID | Задача | Заметка |
|---|---|---|
| P3-1 | EN-паритет постов | наследие v7 V3.1 |
| P3-2 | 4 неиспользуемых компонента: удалить или вернуть в навигацию | ~487 строк |
| P3-3 | `@paper-design/shaders` из `package.json` | известный сирота |
| P3-4 | Токен PostHog в `public/games/*` (11 файлов) | трогать только если игры трогаются |

---

## Владелец (PENDING)

| # | Что | Блокирует |
|---|---|---|
| 1 | Покупка `nikitaboyarkin.com` + DNS, `.ru` как резерв | P1-1, P1-2 |
| 2 | Доступ к Vercel | P1-2 |
| 3 | Утверждение блока «подойдёт, если…» | C4 |
| 4 | PDF-резюме | C8 |
| 5 | Отзывы (если есть в письменном виде) | — |
| 6 | Ведение `docs/contact-log.md` | **гейт** |

Без пункта 6 гейт неизмерим, а значит P0-Content не начинается.

---

## Наследие v7

`prd-v7.md` (тезис «Validate Before You Polish») не отменяется. Его задачи переходят так:

| v7 | v8 |
|---|---|
| Задачи валидации (9 шт., `PENDING`) | Становятся **гейтом** (§3 PRD v8), а не списком задач |
| V1.1 — разговоры с решающими людьми | Порог B гейта: 10 разговоров |
| V1.2 — отклики | Порог A гейта: 20 заявок |
| V3.1 — EN-паритет постов | P3-1 |
| Концепция оффера | §5 PRD v8: оффер не переписывается, точечные правки C3–C5 |
